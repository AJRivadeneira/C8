#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include <alumnos.h>

#define A 10
#define VACIO 0
#define OCUPADO 1
#define ELIMINADO -1

int main()
{
    eAlumno listaAlumnos[A];
    int opcion;#include <alumnos.h>
    int;

    inicializarAlumnos(listaAlumnos, A, VACIO);

    do
    {
        printf("1.Cargar\n2.Mostrar\n3.Salir\nElija opcion: ");
        scanf("%d", &opcion);

        switch(opcion)
        {
            case 1:
                if()
                {

                }
        }
    }
}
